# Learned concepts

Your growing personal reference. Each concept is one `##` section written in the format
defined by the note-format skill (`.claude/skills/note-format/SKILL.md`).

Rules: one section per concept; to update after a re-exercise or exam, EDIT the existing
section — don't duplicate it. Keep notes short; they're reference, not essays.

---

(no concepts yet)
