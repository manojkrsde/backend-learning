---
description: Record the finished session into memory (state, log, concept note)
---

Use the scribe subagent to record this session into memory. Hand it: the task we just completed, the concept(s) I learned, and any key decision or struggle worth remembering — so it can update `ai/memory/state.json`, append to `progress-log.md`, write a concept note via the note-format skill, and update the memory INDEX.
