/** @type {import('next').NextConfig} */
const nextConfig = {
  // The dashboard reads files from the parent repo at runtime, so nothing to
  // configure for output. Kept minimal on purpose.
  reactStrictMode: true,
};

export default nextConfig;
